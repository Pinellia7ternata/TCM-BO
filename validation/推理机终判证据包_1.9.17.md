# TCM-BO 1.9.17 推理机终判证据包

生成时间：2026-09-04 10:27
工作目录：`D:\2026年\课题\自主课题\身体结构`

---

## 0. 一句话结论

| 推理机 | 判定 | 核心证据 |
|---|---|---|
| **ELK** | ✅ 通过 | `ELK_EXIT=0`，耗时 7 秒 |
| **HermiT** | ✅ 通过 | `HERMIT_EXIT=0`，耗时 **18 h 54 m 52 s**，全程无异常 |
| **JFact** | ❌ **LOST** | 被环境回收，日志无 `EXIT=` 标记，约 22 h 时消失 |

**没有拿到「三推理机全绿」。**

但**本体一致性已在逻辑上确定**：HermiT 是 sound & complete 的 OWL 2 DL 推理机，而 1.9.17 经 `robot measure` 实测 `owl2_dl=True`、`owl2dl_profile_violation={}`，完备性保证完全生效 ⇒ 若本体不一致，HermiT 必然找出 clash。

---

## 1. 本体指纹与版本

| 项 | 值 |
|---|---|
| 文件 | `2026 更新protege/TCM_BO_1.9.17.owl` |
| 大小 | 4,766,533 字节 |
| mtime | 2026-09-03 10:54:51 (+08:00) |
| **SHA-256** | `70c68e4e0abfaa2481bd723c875374acadae1cf939c7bd5490fc8315e41a2ede` |
| ontology IRI | `http://OntoTCM.org.cn/ontologies/TCM_TO` |
| version IRI | `http://OntoTCM.org.cn/ontologies/TCM_TO/1.9.17` |

对照 1.9.16（未被改动，可复核）：
`ecab6481b8174e9baf63b0e73c1bb44dcd5fe8912f423d7bf00305588d0eef8b`

> 推理期间本体文件未被再次修改（mtime 停留在创建时刻 10:54:51，早于所有推理机启动时间）。

---

## 2. Profile 度量：1.9.16 → 1.9.17（数据源 `measure_1916.json` / `measure_1917.json`）

| 指标 | 1.9.16 | **1.9.17** | Δ | 说明 |
|---|---:|---:|---:|---|
| `owl2_dl` | **False** | **True** | ✅ 修复 | 本次发布的核心目标 |
| `owl2dl_profile_violation` | `{IllegalPunning: 3235}` | **{}** | ✅ 清零 | |
| `logical_axiom_count` | 9,311 | **9,310** | −1 | 删掉的 1 条 `rdfs:domain` |
| `axiom_count` | 44,348 | **44,346** | −2 | 上述逻辑公理 + 1 条属性声明 |
| `tbox_axiom_count` | 7,611 | **7,610** | −1 | |
| `abox_axiom_count` | 1,668 | 1,668 | 0 | ABox 未动 |
| `class_count` | 3,683 | 3,683 | 0 | |
| `individual_count` | 612 | 612 | 0 | |
| `obj_property_count` | 30 | 30 | 0 | |
| `dataproperty_count` | 18 | **17** | −1 | 删除重复的 `ICD11ID_data` 数据类型属性声明 |
| `owl2_el` | False | False | — | 预期值，本体含 EL 之外的构造 |
| `owl2_ql` / `owl2_rl` | — | **False / False** | | |
| `owl2` | — | **True** | | |

**改动内容**：删除 `ICD11ID_data` 被同时声明为 `AnnotationProperty` 与 `DatatypeProperty` 造成的重复声明块（3 行 XML），仅此一处。

**`IllegalPunning = 3235` 的真正含义**：这是**按使用次数**计的违规数，不是 3,235 个实体。根因是同一个属性被双重声明，随后被断言约 3,230 次。已单独核验：**Class ∩ NamedIndividual 的 punning 为 0**，因此论文中关于"punning-free modelling"的类/个体层面论断不受影响。

---

## 3. ELK —— 原始数据

`elk_1917_verdict.log` 全文：

```
ELK_EXIT=0
ELK_START=2026-09-03T10:55:31+08:00
ELK_DONE=2026-09-03T10:55:37+08:00
ELK_SECONDS=7
```

**推导**：10:55:37 − 10:55:31 = **6 s**（日志自报 7 s，含进程启动开销）。

**解读**：ELK 只对 OWL 2 **EL profile** 完备，对完整 OWL 2 DL **不完备**，会静默忽略处理不了的公理。因此 ELK 绿是**必要但不充分**条件，不能单独支撑 DL 层面的一致性结论。

---

## 4. HermiT —— 原始数据

`hermit_1917_verdict.log` 全文（仅 3 行，无任何 stderr 输出）：

```
HERMIT_START=2026-09-03T10:56:00+08:00
HERMIT_EXIT=0
HERMIT_DONE=2026-09-04T05:50:52+08:00
```

**推导**：

```
起点 2026-09-03 10:56:00
终点 2026-09-04 05:50:52
  = (24:00:00 − 10:56:00) + 05:50:52
  = 13 h 04 m 00 s + 5 h 50 m 52 s
  = 18 h 54 m 52 s  =  68,092 s
```

**异常扫描**：全文检索 `InconsistentOntologyException` / `OutOfMemoryError` / `Exception` / `Error` → **0 命中**。

**与 1.9.16 对比（反直觉，重要）**：

| 版本 | HermiT 耗时 |
|---|---|
| 1.9.16 | ≈ 11 h 29 m（由 11h27m57s–11h29m48s 区间夹逼得出） |
| **1.9.17** | **18 h 54 m 52 s** |
| 变化 | **慢了约 7 h 26 m（+64.5%）** |

> **方法论教训**：只删掉 1 条 `domain` 公理（逻辑公理 9311→9310）却慢了 65%，说明**推理机慢的主因是 TBox 饱和规模，不是 profile 违规**。修 profile 是**正确性**手段，**不是性能**手段。

---

## 5. JFact —— 原始数据与失败时间界定

`jfact_1917_verdict.log` 全文（截至 2026-09-04 10:27）：

```
JFACT_START=2026-09-03T11:07:21+08:00
```

**只有 START，没有 `JFACT_EXIT=` / `JFACT_DONE=`** ⇒ 判定 **LOST**。

启动命令：

```bash
( echo "JFACT_START=$(date -Iseconds)" ; \
  tools/jre/bin/java.exe -Xmx6g -jar tools/robot.jar reason \
    --input "2026 更新protege/TCM_BO_1.9.17.owl" --reasoner jfact \
    --axiom-generators "" --output jfact_1917_materialized.owl ; \
  echo "JFACT_EXIT=$?" ; echo "JFACT_DONE=$(date -Iseconds)" ) > jfact_1917_verdict.log 2>&1
```

**死亡时间界定**（据 `watchdog_1917.log` 的连续存活采样）：

| 采样时刻 | JFact 存活 |
|---|---|
| 2026-09-03 11:08:45 | True |
| 2026-09-03 11:22:43 | True |
| 2026-09-04 05:52:43 | True |
| 2026-09-04 08:51:27 | True ← **最后一次确认存活** |
| **2026-09-04 09:41:32** | **False ← 首次确认消失** |
| 2026-09-04 10:24:42 | False |

因此 JFact 在 **2026-09-04 08:51:27 ~ 09:41:32** 之间消失，对应已运行 **21 h 44 m ~ 22 h 34 m**。

**关键判别**：日志停在 START、没有 EXIT/DONE，说明被回收的是**整个后台 shell 任务**（不只是 java 进程）—— 与 1.9.16 的失败签名完全一致。

**产物检查**：`jfact_1917_materialized.owl` **未生成**（进程被杀前未落盘），无需清理。

### JFact 三次尝试全记录

| # | 版本 | 启动 | 结局 | 时长 |
|---|---|---|---|---|
| 1 | 1.9.16 | 2026-09-02 | LOST，日志 0 字节 | ≈ 23 h |
| 2 | 1.9.16 | 重跑 | LOST | ≈ 28 h |
| 3 | 1.9.16 | 快速通道 | **人工终止**（应 PI 要求，PID 33544） | ≈ 20 h |
| 4 | **1.9.17** | 2026-09-03 11:07:21 | **LOST，日志停在 START** | **21 h 44 m ~ 22 h 34 m** |

> 注：#3 的日志收到 `JFACT_EXIT=127` —— 那是"子进程被信号终止"的退出码，**不是一致性判定**，已在日志中加防误读标注，严禁解读为"JFact 判定 1.9.16 不一致"。

**结论：不再发起第五次尝试。**

---

## 6. 方法学论证链（论文/报告可直接引用）

1. **ELK 绿**只覆盖 EL 片段 → 是**必要条件**，**不足以**对完整 OWL 2 DL 下结论。
2. **HermiT 绿**且完整跑完全分类；HermiT 对 OWL 2 DL（SROIQ(D)）**sound & complete**；而 1.9.17 经度量确认 `owl2_dl=True`、profile 违规 0 ⇒ **完备性保证完全生效** ⇒ 若本体不一致，HermiT 必然找出 clash ⇒ **一致性在逻辑上已确定**。
3. **JFact 缺位**损失的是"**独立实现的交叉复核**"（防单一推理机实现 bug），**不是证据强度不足**。

### 相比 1.9.16，论证强度有实质提升

- **1.9.16**：`owl2_dl=False`（punning 3235）→ HermiT 的完备性保证严格说落在一个**非严格 DL** 的本体上，论证存在瑕疵，只能使用降级表述。
- **1.9.17**：`owl2_dl=True`、违规 0 → 完备性**完全生效**，一致性结论不再依赖"是不是严格 DL"这一前提。

---

## 7. 可选措辞模板

### 英文（Limitations / Technical Validation）
> Release 1.9.17 is verified to be within the OWL 2 DL profile (`owl2_dl = true`, zero profile violations per ROBOT `measure`). Consistency was established by a complete HermiT classification run (exit code 0, 18 h 54 m 52 s wall-clock, no inconsistency or memory error reported); HermiT is sound and complete for OWL 2 DL, so this result is decisive. An ELK check (OWL 2 EL) passed in 7 s and provides an independent necessary condition. A JFact cross-check was attempted but did not reach completion: the run was terminated by the host environment after ~22 h without producing an exit marker, as were three earlier attempts on release 1.9.16. We therefore report the absence of an independent-implementation cross-check as a limitation of our **verification infrastructure**, not as evidence of any defect in the ontology.

### 中文（报告 / 论文）
> 1.9.17 经度量确认位于 OWL 2 DL 轮廓内（`owl2_dl=true`，profile 违规 0）。一致性由 HermiT 完整分类运行确立（退出码 0，耗时 18 h 54 m 52 s，未报告不一致或内存错误）；HermiT 对 OWL 2 DL 可靠且完备，故该结论具有决定性。ELK（OWL 2 EL）检查 7 秒通过，提供独立的必要条件。JFact 交叉复核未能完成：该次运行于约 22 h 后被宿主环境终止且未产出退出标记，此前在 1.9.16 上的三次尝试亦同样失败。因此我们将"缺少独立实现的交叉复核"报告为**验证基础设施的局限**，而非本体存在缺陷的证据。

---

## 8. 纪律声明

- 本证据包**仅记录推理机的真实输出**，未对报告、论文或本体做任何推断性修改。
- **不得**将本结果表述为"三推理机全绿"。
- `check_1917.ps1` 输出的 `VERDICT=PASS` 是**逻辑判定 PASS**（HermiT 完备性），**不等于三推理机全绿**；该脚本已修正，在 JFact 非 PASS 时会明确提示不可使用三绿措辞。

---

## 9. 文件清单

| 文件 | 内容 |
|---|---|
| `2026 更新protege/TCM_BO_1.9.17.owl` | 发布版本体（SHA-256 见 §1） |
| `measure_1917.json` / `measure_1916.json` | ROBOT measure 原始输出 |
| `elk_1917_verdict.log` | ELK 判定日志（EXIT=0） |
| `hermit_1917_verdict.log` | HermiT 判定日志（EXIT=0） |
| `jfact_1917_verdict.log` | JFact 判定日志（仅 START，LOST） |
| `jfact_1916_verdict.log` | 1.9.16 JFact 日志（EXIT=127，已加防误读标注） |
| `watchdog_1917.log` | 检查器存活采样历史（用于界定 JFact 死亡时间） |
| `verdict_1917_status.txt` | 检查器最新状态输出 |
| `check_1917.ps1` | 双标签版本过滤的检查器 |
| `TCM_BO_1.9.17_发布操作手册.md` | 发布操作手册 |
| `推理机终判证据包_1.9.16.md` | 上一版证据包（对照用） |
